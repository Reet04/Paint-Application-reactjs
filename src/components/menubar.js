import React from "react";

const menubar=()=>{
    return(
    <div>
    <canvas 
      style={{
        border:"2px solid #000",
        padding: "2px",
        margin: "5px"

      }}
      width={600}
      height={30}
    />
    </div>);
};

export default menubar;